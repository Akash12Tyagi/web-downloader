{
  "name": "mediahub-downloader",
  "version": "1.0.0",
  "private": true,
  "description": "MediaHub Downloader — premium YouTube & Instagram media downloader (Next.js + Express + yt-dlp/ffmpeg)",
  "scripts": {
    "dev": "concurrently -n backend,frontend -c green,cyan \"npm:dev --prefix backend\" \"npm:dev --prefix frontend\"",
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "build": "npm run build --prefix backend && npm run build --prefix frontend",
    "docker:up": "docker compose up --build",
    "docker:down": "docker compose down",
    "format": "prettier --write \"backend/src/**/*.ts\" \"frontend/src/**/*.{ts,tsx}\"",
    "format:check": "prettier --check \"backend/src/**/*.ts\" \"frontend/src/**/*.{ts,tsx}\""
  },
  "devDependencies": {
    "concurrently": "^9.1.0",
    "prettier": "^3.3.3"
  }
}
